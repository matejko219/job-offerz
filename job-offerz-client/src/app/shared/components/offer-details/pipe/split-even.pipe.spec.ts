import { SplitEvenPipe } from './split-even.pipe';

describe('SplitEvenPipe', () => {
  it('create an instance', () => {
    const pipe = new SplitEvenPipe();
    expect(pipe).toBeTruthy();
  });
});
