import { StarHoverDirective } from './star-hover.directive';

describe('StarHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new StarHoverDirective();
    expect(directive).toBeTruthy();
  });
});
