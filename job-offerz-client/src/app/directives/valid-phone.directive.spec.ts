import { ValidPhoneDirective } from './valid-phone.directive';

describe('ValidPhoneDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidPhoneDirective();
    expect(directive).toBeTruthy();
  });
});
