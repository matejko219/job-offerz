import { OptionHasIdDirective } from './option-has-id.directive';

describe('OptionHasIdDirective', () => {
  it('should create an instance', () => {
    const directive = new OptionHasIdDirective();
    expect(directive).toBeTruthy();
  });
});
