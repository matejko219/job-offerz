import { ValidUrlDirective } from './valid-url.directive';

describe('ValidUrlDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidUrlDirective();
    expect(directive).toBeTruthy();
  });
});
